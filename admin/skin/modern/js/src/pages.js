/*const pages = {
    run : function(){
        if($.fn.bootstrapSelect !== undefined) {
            if($('#parent_id').val() !== ''){
                let id = $('#parent_id').val();
                let cus = $('#filter-pages').find('li[data-value="'+id+'"]');
                if(!cus.length) {
                    $('#parent').bootstrapSelect('clear');
                } else {
                    let cu = $(cus[0]);
                    $('#parent').bootstrapSelect('select',cu);
                }
            }
        }
    }
};

window.addEventListener('load',function(){
    pages.run();
});*/
